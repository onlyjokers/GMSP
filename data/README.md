# Data Layout

The project stores research datasets under `data/`.

Current convention:

- `data/external/`: datasets downloaded or imported from external sources

This repository currently keeps the active research datasets under `data/external/`.
